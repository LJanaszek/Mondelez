const CONFIG = {
    // Z jakiej odległości można zaliczyć punkt
    GEO_POINT_DISTANCE: 40, // m

    // Minimalna dokładność GPS aby gra działała
    MIN_GEO_ACCURACY: 200, // m
}

export default CONFIG;
